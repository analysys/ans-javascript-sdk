(function () {
  'use strict';

  var globalWindow = window;

  var typeMap = {};
  var typeArr = ['Boolean', 'Number', 'String', 'Function', 'Array', 'Date', 'RegExp', 'Object', 'Error', 'HTMLCollection'];
  typeArr.map(function (item) {
      typeMap["[object " + item + "]"] = item.toLowerCase();
  });
  function type(value) {
      var typeName = typeof value;
      if (typeName === 'object') {
          return typeMap[Object.prototype.toString.call(value)];
      }
      return typeName;
  }
  function isNumber(value) {
      return type(value) === 'number';
  }
  function isString(value) {
      return type(value) === 'string';
  }
  function isArray(value) {
      return Array.isArray(value);
  }
  function isObject(value) {
      return type(value) === 'object';
  }
  function isFunction(value) {
      return type(value) === 'function';
  }
  var isElement = function (value) { return value instanceof HTMLElement; };
  function isHtmlCollection(value) {
      return type(value) === 'htmlcollection';
  }
  var isSvg = function (value) { return value instanceof SVGElement; };

  /**
   * 对象浅拷贝
   *
   */
  function assign(target) {
      var args = [];
      for (var _i = 1; _i < arguments.length; _i++) {
          args[_i - 1] = arguments[_i];
      }
      for (var i = 0; i < args.length; i++) {
          var item = args[i];
          if (isObject(item)) {
              for (var key in item) {
                  target[key] = item[key];
              }
          }
      }
      return target;
  }

  /**
   * 获取元素属性
   * @param el
   * @param attrName
   * @returns
   */
  var getElementAttr = function (el, attrName) {
      return el && el.getAttribute ? el.getAttribute(attrName) : null;
  };
  // 获取元素内容
  function getElementContent(ele) {
      var eleContent = '';
      var tagName = ele.tagName.toLowerCase();
      if (tagName === 'input' && ['button', 'submit'].indexOf(ele.type) > -1) {
          eleContent = ele.value || '';
      }
      else if (tagName === 'img') {
          eleContent = getElementAttr(ele, 'alt') || getElementAttr(ele, 'title') || '';
      }
      else {
          eleContent = ele.textContent;
      }
      return stringSlice(eleContent.trim());
  }
  /**
   *  从当前节点往上遍历dom结构，返回满足条件的第一个dom
   * @param el
   * @param fn
   * @param option.root 指定根节点
   * @returns
   */
  var elementFind = function (el, fn, option) {
      var opt = assign({}, option);
      var index = 0;
      if (isElement(el) || isSvg(el)) {
          while (el.parentNode) {
              if (fn(el, index)) {
                  return el;
              }
              index++;
              el = el.parentNode;
              if (opt.root && el === opt.root) {
                  fn && fn(el, index);
                  break;
              }
          }
      }
  };
  /**
   * 获取dom节点的xpath
   * @param el 节点
   * @param option.full 从根节点html开始
   * @param option.mode 生成模式 默认宽松模式
   * @param option.endTag 结束标签，向上遍历时遇到该标签以它作为结尾
   */
  var getElementXpath = function (el, option) {
      var opt = assign({ full: false, mode: 'loose', endTag: [] }, option);
      var path = '';
      elementFind(el, function (o) {
          var id = o.id;
          var tagName = o.tagName.toLowerCase();
          var str = '';
          if (opt.endTag.indexOf(tagName) > -1) {
              path = '';
          }
          if (isElement(o)) {
              if (id && !opt.full) {
                  str = "//" + tagName + "[@id=\"" + id + "\"]";
              }
              else {
                  var nodes = o.parentNode
                      ? Array.from(o.parentNode.children).filter(function (v) {
                          var isTag = v.tagName === o.tagName;
                          var className = v.className;
                          if (className && isString(className)) {
                              if (opt.mode === 'ordinary')
                                  return isTag && className.indexOf(o.classList[0]) > -1;
                              if (opt.mode === 'strict')
                                  return isTag && className === o.className;
                          }
                          return isTag;
                      })
                      : [];
                  var index = nodes.length > 1 ? "[" + (nodes.indexOf(o) + 1) + "]" : '';
                  var className = '';
                  // 半严格模式
                  if (opt.mode === 'ordinary') {
                      if (o.classList[0]) {
                          className = "[contains(@class,\"" + o.classList[0] + "\")]";
                      }
                  }
                  // 严格模式
                  if (opt.mode === 'strict') {
                      if (o.className) {
                          className = "[@class=\"" + o.className + "\"]";
                      }
                  }
                  str = "/" + tagName + className + index;
              }
              path = str + path;
          }
          if (!opt.full && id)
              return true;
      });
      return path;
  };

  // 字符串超过255截取
  function stringSlice(str) {
      if (isString(str) && str.length > 255) {
          return str.slice(0, 254) + '$';
      }
      return str;
  }

  // 获取配置曝光元素有效列表
  function getConfigEles(elements) {
      var list = [];
      if (isArray(elements)) {
          for (var i = 0; i < elements.length; i++) {
              var itemEl = elements[i];
              if (isElement(itemEl)) {
                  list.push(itemEl);
              }
              if (isHtmlCollection(itemEl)) {
                  for (var j = 0; j < itemEl.length; j++) {
                      list.push(itemEl[j]);
                  }
              }
          }
      }
      return list;
  }
  // 判断是否是通过配置配置的曝光元素
  function isConfigExposureEle(elements, ele) {
      if (elements) {
          if (isElement(elements) && elements === ele) {
              return true;
          }
          if (isArray(elements)) {
              for (var i = 0; i < elements.length; i++) {
                  var itemEl = elements[i];
                  if (isElement(itemEl) && itemEl === ele) {
                      return true;
                  }
                  if (isHtmlCollection(itemEl)) {
                      for (var j = 0; j < itemEl.length; j++) {
                          if (itemEl[j] === ele) {
                              return true;
                          }
                      }
                  }
              }
          }
          if (isFunction(elements)) {
              var res = elements();
              return isConfigExposureEle(res, ele);
          }
      }
      return false;
  }
  // 判断是否是通过属性标示的曝光元素
  function isExposureEle(ele) {
      if ((getElementAttr(ele, 'data-ark-exposure') !== null))
          return true;
      return false;
  }

  /*
  md5.js
  */
  function MD5(str, bit) {
      var sMessage = str;
      function RotateLeft(lValue, iShiftBits) { return (lValue << iShiftBits) | (lValue >>> (32 - iShiftBits)); }
      function AddUnsigned(lX, lY) {
          var lX4, lY4, lX8, lY8, lResult;
          lX8 = (lX & 0x80000000);
          lY8 = (lY & 0x80000000);
          lX4 = (lX & 0x40000000);
          lY4 = (lY & 0x40000000);
          lResult = (lX & 0x3FFFFFFF) + (lY & 0x3FFFFFFF);
          if (lX4 & lY4)
              return (lResult ^ 0x80000000 ^ lX8 ^ lY8);
          if (lX4 | lY4) {
              if (lResult & 0x40000000)
                  return (lResult ^ 0xC0000000 ^ lX8 ^ lY8);
              else
                  return (lResult ^ 0x40000000 ^ lX8 ^ lY8);
          }
          else
              return (lResult ^ lX8 ^ lY8);
      }
      function F(x, y, z) { return (x & y) | ((~x) & z); }
      function G(x, y, z) { return (x & z) | (y & (~z)); }
      function H(x, y, z) { return (x ^ y ^ z); }
      function I(x, y, z) { return (y ^ (x | (~z))); }
      function FF(a, b, c, d, x, s, ac) {
          a = AddUnsigned(a, AddUnsigned(AddUnsigned(F(b, c, d), x), ac));
          return AddUnsigned(RotateLeft(a, s), b);
      }
      function GG(a, b, c, d, x, s, ac) {
          a = AddUnsigned(a, AddUnsigned(AddUnsigned(G(b, c, d), x), ac));
          return AddUnsigned(RotateLeft(a, s), b);
      }
      function HH(a, b, c, d, x, s, ac) {
          a = AddUnsigned(a, AddUnsigned(AddUnsigned(H(b, c, d), x), ac));
          return AddUnsigned(RotateLeft(a, s), b);
      }
      function II(a, b, c, d, x, s, ac) {
          a = AddUnsigned(a, AddUnsigned(AddUnsigned(I(b, c, d), x), ac));
          return AddUnsigned(RotateLeft(a, s), b);
      }
      function ConvertToWordArray(sMessage) {
          var lWordCount;
          var lMessageLength = sMessage.length;
          var lNumberOfWords_temp1 = lMessageLength + 8;
          var lNumberOfWords_temp2 = (lNumberOfWords_temp1 - (lNumberOfWords_temp1 % 64)) / 64;
          var lNumberOfWords = (lNumberOfWords_temp2 + 1) * 16;
          var lWordArray = Array(lNumberOfWords - 1);
          var lBytePosition = 0;
          var lByteCount = 0;
          while (lByteCount < lMessageLength) {
              lWordCount = (lByteCount - (lByteCount % 4)) / 4;
              lBytePosition = (lByteCount % 4) * 8;
              lWordArray[lWordCount] = (lWordArray[lWordCount] | (sMessage.charCodeAt(lByteCount) << lBytePosition));
              lByteCount++;
          }
          lWordCount = (lByteCount - (lByteCount % 4)) / 4;
          lBytePosition = (lByteCount % 4) * 8;
          lWordArray[lWordCount] = lWordArray[lWordCount] | (0x80 << lBytePosition);
          lWordArray[lNumberOfWords - 2] = lMessageLength << 3;
          lWordArray[lNumberOfWords - 1] = lMessageLength >>> 29;
          return lWordArray;
      }
      function WordToHex(lValue) {
          var WordToHexValue = "", WordToHexValue_temp = "", lByte, lCount;
          for (lCount = 0; lCount <= 3; lCount++) {
              lByte = (lValue >>> (lCount * 8)) & 255;
              WordToHexValue_temp = "0" + lByte.toString(16);
              WordToHexValue = WordToHexValue + WordToHexValue_temp.substr(WordToHexValue_temp.length - 2, 2);
          }
          return WordToHexValue;
      }
      var x = [];
      var k, AA, BB, CC, DD, a, b, c, d;
      var S11 = 7, S12 = 12, S13 = 17, S14 = 22;
      var S21 = 5, S22 = 9, S23 = 14, S24 = 20;
      var S31 = 4, S32 = 11, S33 = 16, S34 = 23;
      var S41 = 6, S42 = 10, S43 = 15, S44 = 21;
      // Steps 1 and 2. Append padding bits and length and convert to words 
      x = ConvertToWordArray(sMessage);
      // Step 3. Initialise 
      a = 0x67452301;
      b = 0xEFCDAB89;
      c = 0x98BADCFE;
      d = 0x10325476;
      // Step 4. Process the message in 16-word blocks 
      for (k = 0; k < x.length; k += 16) {
          AA = a;
          BB = b;
          CC = c;
          DD = d;
          a = FF(a, b, c, d, x[k + 0], S11, 0xD76AA478);
          d = FF(d, a, b, c, x[k + 1], S12, 0xE8C7B756);
          c = FF(c, d, a, b, x[k + 2], S13, 0x242070DB);
          b = FF(b, c, d, a, x[k + 3], S14, 0xC1BDCEEE);
          a = FF(a, b, c, d, x[k + 4], S11, 0xF57C0FAF);
          d = FF(d, a, b, c, x[k + 5], S12, 0x4787C62A);
          c = FF(c, d, a, b, x[k + 6], S13, 0xA8304613);
          b = FF(b, c, d, a, x[k + 7], S14, 0xFD469501);
          a = FF(a, b, c, d, x[k + 8], S11, 0x698098D8);
          d = FF(d, a, b, c, x[k + 9], S12, 0x8B44F7AF);
          c = FF(c, d, a, b, x[k + 10], S13, 0xFFFF5BB1);
          b = FF(b, c, d, a, x[k + 11], S14, 0x895CD7BE);
          a = FF(a, b, c, d, x[k + 12], S11, 0x6B901122);
          d = FF(d, a, b, c, x[k + 13], S12, 0xFD987193);
          c = FF(c, d, a, b, x[k + 14], S13, 0xA679438E);
          b = FF(b, c, d, a, x[k + 15], S14, 0x49B40821);
          a = GG(a, b, c, d, x[k + 1], S21, 0xF61E2562);
          d = GG(d, a, b, c, x[k + 6], S22, 0xC040B340);
          c = GG(c, d, a, b, x[k + 11], S23, 0x265E5A51);
          b = GG(b, c, d, a, x[k + 0], S24, 0xE9B6C7AA);
          a = GG(a, b, c, d, x[k + 5], S21, 0xD62F105D);
          d = GG(d, a, b, c, x[k + 10], S22, 0x2441453);
          c = GG(c, d, a, b, x[k + 15], S23, 0xD8A1E681);
          b = GG(b, c, d, a, x[k + 4], S24, 0xE7D3FBC8);
          a = GG(a, b, c, d, x[k + 9], S21, 0x21E1CDE6);
          d = GG(d, a, b, c, x[k + 14], S22, 0xC33707D6);
          c = GG(c, d, a, b, x[k + 3], S23, 0xF4D50D87);
          b = GG(b, c, d, a, x[k + 8], S24, 0x455A14ED);
          a = GG(a, b, c, d, x[k + 13], S21, 0xA9E3E905);
          d = GG(d, a, b, c, x[k + 2], S22, 0xFCEFA3F8);
          c = GG(c, d, a, b, x[k + 7], S23, 0x676F02D9);
          b = GG(b, c, d, a, x[k + 12], S24, 0x8D2A4C8A);
          a = HH(a, b, c, d, x[k + 5], S31, 0xFFFA3942);
          d = HH(d, a, b, c, x[k + 8], S32, 0x8771F681);
          c = HH(c, d, a, b, x[k + 11], S33, 0x6D9D6122);
          b = HH(b, c, d, a, x[k + 14], S34, 0xFDE5380C);
          a = HH(a, b, c, d, x[k + 1], S31, 0xA4BEEA44);
          d = HH(d, a, b, c, x[k + 4], S32, 0x4BDECFA9);
          c = HH(c, d, a, b, x[k + 7], S33, 0xF6BB4B60);
          b = HH(b, c, d, a, x[k + 10], S34, 0xBEBFBC70);
          a = HH(a, b, c, d, x[k + 13], S31, 0x289B7EC6);
          d = HH(d, a, b, c, x[k + 0], S32, 0xEAA127FA);
          c = HH(c, d, a, b, x[k + 3], S33, 0xD4EF3085);
          b = HH(b, c, d, a, x[k + 6], S34, 0x4881D05);
          a = HH(a, b, c, d, x[k + 9], S31, 0xD9D4D039);
          d = HH(d, a, b, c, x[k + 12], S32, 0xE6DB99E5);
          c = HH(c, d, a, b, x[k + 15], S33, 0x1FA27CF8);
          b = HH(b, c, d, a, x[k + 2], S34, 0xC4AC5665);
          a = II(a, b, c, d, x[k + 0], S41, 0xF4292244);
          d = II(d, a, b, c, x[k + 7], S42, 0x432AFF97);
          c = II(c, d, a, b, x[k + 14], S43, 0xAB9423A7);
          b = II(b, c, d, a, x[k + 5], S44, 0xFC93A039);
          a = II(a, b, c, d, x[k + 12], S41, 0x655B59C3);
          d = II(d, a, b, c, x[k + 3], S42, 0x8F0CCC92);
          c = II(c, d, a, b, x[k + 10], S43, 0xFFEFF47D);
          b = II(b, c, d, a, x[k + 1], S44, 0x85845DD1);
          a = II(a, b, c, d, x[k + 8], S41, 0x6FA87E4F);
          d = II(d, a, b, c, x[k + 15], S42, 0xFE2CE6E0);
          c = II(c, d, a, b, x[k + 6], S43, 0xA3014314);
          b = II(b, c, d, a, x[k + 13], S44, 0x4E0811A1);
          a = II(a, b, c, d, x[k + 4], S41, 0xF7537E82);
          d = II(d, a, b, c, x[k + 11], S42, 0xBD3AF235);
          c = II(c, d, a, b, x[k + 2], S43, 0x2AD7D2BB);
          b = II(b, c, d, a, x[k + 9], S44, 0xEB86D391);
          a = AddUnsigned(a, AA);
          b = AddUnsigned(b, BB);
          c = AddUnsigned(c, CC);
          d = AddUnsigned(d, DD);
      }
      if (bit === 32) {
          return WordToHex(a) + WordToHex(b) + WordToHex(c) + WordToHex(d);
      }
      return WordToHex(b) + WordToHex(c);
  }

  var AnalysysAgent = globalWindow.AnalysysAgent;
  var exposureConfig = {
      valid_time: 300,
      property: {},
      exposure_click: false,
      multiple: false,
      element_list: [],
  };
  // 记录曝光元素
  var exposureElMap = new WeakMap();
  // 创建dom结构改变监听
  var mutation = new MutationObserver(function (mutations) {
      for (var i = 0; i < mutations.length; i++) {
          var addedNodes = mutations[i].addedNodes;
          addIo(addedNodes);
      }
  });
  // 创建与视界相交监听
  var io = new IntersectionObserver(function (entries) {
      var _loop_1 = function (i) {
          var item = entries[i];
          var ele = item.target;
          // 显示到可视区域
          if (item.intersectionRatio > 0) {
              if (exposureElMap.get(ele) === false) {
                  var time = setTimeout(function () {
                      send(ele);
                  }, exposureConfig.valid_time);
                  exposureElMap.set(ele, time);
              }
          }
          // 从可视区域离开
          if (item.intersectionRatio === 0) {
              var n = exposureElMap.get(ele);
              if (isNumber(n)) {
                  clearTimeout(n);
                  exposureElMap.set(ele, false);
              }
          }
      };
      for (var i = 0; i < entries.length; i++) {
          _loop_1(i);
      }
  }, {
      threshold: [0.01]
  });
  // 添加元素交叉观察
  function addIo(eles, isVerify) {
      for (var i = 0; i < eles.length; i++) {
          var ele = eles[i];
          if (ele.nodeType === 1) {
              if (!exposureElMap.has(ele) && (isExposureEle(ele) || isConfigExposureEle(exposureConfig.element_list, ele))) {
                  io.observe(ele);
                  exposureElMap.set(ele, false);
              }
              if (ele.childNodes.length > 0) {
                  addIo(ele.childNodes);
              }
          }
      }
  }
  function send(ele) {
      // 获取元素上曝光属性
      var exposureAttr = getElementAttr(ele, 'data-ark-exposure');
      if (exposureAttr) {
          try {
              exposureAttr = JSON.parse(exposureAttr) || {};
          }
          catch (e) {
              exposureAttr = {};
          }
      }
      else {
          exposureAttr = {};
      }
      // 获取exposure_id
      if (!exposureAttr.exposure_id) {
          var xpath = getElementXpath(ele);
          exposureAttr['exposure_id'] = MD5(xpath);
      }
      // 获取曝光文本
      if (!exposureAttr.exposure_content) {
          exposureAttr['exposure_content'] = getElementContent(ele);
      }
      var exposureAttrs = assign({}, exposureConfig.property, exposureAttr);
      AnalysysAgent.track('exposure_points', exposureAttrs);
      ele['exposureAttrs'] = exposureAttrs;
      if (!exposureConfig.multiple) {
          exposureElMap.set(ele, true);
          io.unobserve(ele);
      }
      // xx毫秒后可以再次曝光
      if (exposureConfig.multiple > 0) {
          exposureElMap.set(ele, true);
          setTimeout(function () {
              exposureElMap.set(ele, false);
          }, exposureConfig.multiple);
      }
  }
  function init() {
      assign(exposureConfig, AnalysysAgent.config.exposure);
      addIo(getConfigEles(exposureConfig.element_list));
      addIo(document.querySelectorAll('[data-ark-exposure]'));
      mutation.observe(document.body, {
          childList: true,
          subtree: true,
      });
      // 采集曝光点点击事件
      if (exposureConfig.exposure_click) {
          document.addEventListener("click", function (e) {
              var el = e.target;
              var exposureAttrs = el['exposureAttrs'];
              if (exposureAttrs && !el['isExposureClick']) {
                  AnalysysAgent.track('exposure_click', exposureAttrs);
                  el['isExposureClick'] = true;
              }
          });
      }
  }
  var href = globalWindow.location.href;
  if (AnalysysAgent && href.indexOf('visual=true') < 0 && href.indexOf('arkheatmap=true') < 0) {
      // sdk初始化未完成
      !AnalysysAgent.isInit ? AnalysysAgent.on('afterInit', init) : init();
  }

}());
